#include "main.h"

/**
 * puts2 - prints characters in a string every skip interval
 * @str: input string
 * Return: void
 */

void puts2(char *str)
{
	int i;

	for (i = 0; i <= '\0', i + 2)
	{
		_putchar(*(str + i));
	}
}
